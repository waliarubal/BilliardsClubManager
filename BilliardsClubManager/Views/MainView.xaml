<Window
    x:Class="BilliardsClubManager.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BilliardsClubManager.ViewModels"
    Title="MainView"
    Width="1024"
    Height="768"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ToolBar ToolBarTray.IsLocked="True">
            <ToolBar.Resources>
                <Style TargetType="StackPanel">
                    <Setter Property="Orientation" Value="Horizontal" />
                </Style>
            </ToolBar.Resources>
            <Button>
                <StackPanel>
                    <Image Source="/Assets/Images/dashboard-32.png" />
                    <TextBlock Text="Dashboard" />
                </StackPanel>
            </Button>
            <Button Command="{Binding Path=CreateChildViewCommand, Mode=OneTime}">
                <Button.CommandParameter>
                    <vm:PlayerViewModel />
                </Button.CommandParameter>
                <StackPanel>
                    <Image Source="/Assets/Images/basketball-player-32.png" />
                    <TextBlock Text="Players" />
                </StackPanel>
            </Button>
            <Button Command="{Binding Path=CreateChildViewCommand, Mode=OneTime}">
                <Button.CommandParameter>
                    <vm:TableViewModel />
                </Button.CommandParameter>
                <StackPanel>
                    <Image Source="/Assets/Images/pool-table-32.png" />
                    <TextBlock Text="Tables" />
                </StackPanel>
            </Button>
            <Button>
                <StackPanel>
                    <Image Source="/Assets/Images/combo-chart-32.png" />
                    <TextBlock Text="Reports" />
                </StackPanel>
            </Button>
        </ToolBar>
        <ContentPresenter Grid.Row="1" Content="{Binding Path=ChildView, Mode=OneWay}" />
    </Grid>
</Window>
