<UserControl
    x:Class="BilliardsClubManager.Views.ReportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BilliardsClubManager.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:ReportViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DataGrid
            HeadersVisibility="None"
            IsReadOnly="False"
            ItemsSource="{Binding Path=Parameters, Mode=OneTime}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=First, Mode=OneTime}" Header="Parameter Name" />
                <DataGridTextColumn Binding="{Binding Path=Second, Mode=TwoWay}" Header="Parameter Value" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
